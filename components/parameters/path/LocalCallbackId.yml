name: localCallbackId
description: A local identifier for a callback. It must express at least 128-bits of random information to ensure the full callback URL can be treated as a capability URL.
in: path
required: true
schema:
  type: string

